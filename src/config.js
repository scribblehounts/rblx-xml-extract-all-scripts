const path = require('path');

module.exports = {
  inputDir: path.join(__dirname, '../input'), // Adjust to your actual input folder
  outputDir: path.join(__dirname, '../output') // Use output directory instead of just a single file
};
